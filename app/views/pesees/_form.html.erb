<%= form_for @pesee, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> /Pesee de <%=current_user.email%></legend>

    <div class="control-group">
      <%= f.label :poids, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :poids, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :date, :class => 'control-label' %>
      <div class="controls">
        <%= f.date_select :date, :class => 'date_select' %>
      </div>
    </div>

   

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', pesees_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
