<%= form_for @aliment, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> /Aliment</legend>

    <div class="control-group">
      <%= f.label :name, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :name, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :repa_id, :class => 'control-label' %>
      <div class="controls">
        <%= f.number_field :repa_id, :class => 'number_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :calories, :class => 'control-label' %>
      <div class="controls">
        <%= f.number_field :calories, :class => 'number_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', aliments_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
